jobs:

# jobs for the Nouvola sample application

################################

# add pipeline job
# Run API tests on Nouvola cloud
  - name: test-api-nouvola-run-tests
    type: runSh
    steps:
      - IN: test-api-nouvola-api-params
      - TASK:
        - script: pwd && ls -l
        - script: echo $SHIPPABLE_BUILD_DIR
        - script: echo $SHIPPABLE_REPO_DIR
        - script: ls -l $SHIPPABLE_BUILD_DIR
        - script: ls -l $SHIPPABLE_REPO_DIR
        - script: $SHIPPABLE_BUILD_DIR/run-nouvola-test.sh
        - script: $SHIPPABLE_REPO_DIR/run-nouvola-test.sh
    flags:
      - test-api-nouvola
